import 'package:flutter/material.dart';

class WorkshopsScreen extends StatelessWidget {
  const WorkshopsScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("Tela de Oficinas", style: TextStyle(color: Colors.black)));
}